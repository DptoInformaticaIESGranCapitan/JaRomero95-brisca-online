{% extends 'WreckThemBundle:Page:base.html.twig' %}

{% block body %}
    {% block header %}
        <h1>Wreck Them</h1>
    {% endblock %}

    {% if user %}
        Bienvenido {{ user.email }}

        {% if user.imgPath %}
            <img src="{{ asset('uploads/images' ~ user.imgPath ) }}" alt="Foto de perfil">
        {% else %}
            no image
        {% endif %}

        <a href="{{ path('perfil') }}">Perfil</a>
        <a href="{{ path('logout') }}">Cerrar sesión</a>

        {% include 'WreckThemBundle:Chat:chat.html.twig' %}

        <button id="search-game">Buscar partida</button>
    {% else %}
        <a href="{{ path('login') }}">Entrar</a>
        <a href="{{ path('register') }}">Registrarse</a>
    {% endif %}


    {% block content %}

    {% endblock %}

    {% block footer %}

    {% endblock %}
{% endblock %}